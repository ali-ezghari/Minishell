cmds: 

PASRSING:

=>  hi| : causes segfault   || FIXED
=>  | : causes segfault     || FIXED
=>  > : causes segfault     || FIXED
=>  >> : causes segfault    || FIXED
=> echo $? : doesn't expand || FIXED
=> echo $ : prints nothing  || FIXED
=> echo hi | "|" : causes syntax error but "|" should be treated as a string, so that it can be handled in the exection
=> exit : double free








EXECUTION: 

=> exit : causes double free when a command or more been executed before it. problem in the int main.
=> ./ : Permission denied, not like bash
=> cat << "hi" : should not expand
=> cat << hi : should expand

